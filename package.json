{
  "name": "obsidian-pdf-selection-translator",
  "version": "0.2.1",
  "private": true,
  "description": "LLM selection translation for Obsidian PDF and PDF++",
  "license": "MIT",
  "author": "xfrrn",
  "repository": {
    "type": "git",
    "url": "https://github.com/xfrrn/obsidian-pdf-selection-translator.git"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build && npm run verify-release",
    "verify-release": "node scripts/verify-release.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/jsdom": "^21.1.7",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "obsidian": "^1.8.7",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
